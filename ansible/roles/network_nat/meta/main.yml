---
galaxy_info:
  role_name: network_nat
  author: tomkatom
  description: Outbound NAT (masquerade) for VMs on vmbr1 and for the guest exit VPN, a generic inbound DNAT rule list (network_nat_ingress_rules), and the forward-chain rules that isolate the exit tunnel from the lab — all on the host's single public IP
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions:
        - trixie
dependencies: []
