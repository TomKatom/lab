---
# Role-private vars, prefixed pve_repos_* so they can't collide with another
# role's defaults.

pve_repos_enterprise_sources: /etc/apt/sources.list.d/pve-enterprise.sources
pve_repos_ceph_enterprise_sources: /etc/apt/sources.list.d/ceph.sources
