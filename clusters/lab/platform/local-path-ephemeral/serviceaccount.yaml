# Used by both the provisioner Deployment and, via `--service-account-name`,
# the short-lived helper pods it spawns to mkdir/rm the volume directories.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: local-path-ephemeral
