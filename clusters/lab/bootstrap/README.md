# bootstrap

Argo CD installation (Helm values + ksops Kustomize plugin patch on the
repo-server) and the `root-app.yaml` app-of-apps entrypoint that points Argo
at `platform/` and `apps/`.

Not yet implemented — built in Phase 4. Procedure will be documented in
`docs/bootstrap.md` once written.
